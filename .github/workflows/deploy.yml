name: Deploy Static Site

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ VM_HOST.VM_HOST }}
          username: ${{ VM_HOST.VM_USER }}
          key: ${{ VM_HOST.DEPLOY_KEY }}
          script: |
            bash /home/azureuser/deploy.sh
